{% extends 'admin_base.html.twig' %}

{% block body %}


  <div class="row">

    <div class="col-md-6">

        <div class="card">

            <div class="card-header bg-dark font-white">
                <h4>Add new user</h4>
            </div>

            <div class="card-body">

            {{ form(form) }}

            </div>
        </div>

    </div>


      <div class="col-md-6">

          <div class="card">

              <div class="card-header bg-dark font-white">
                  <h4>Admin users</h4>
              </div>


              <div class="card-body">

                  <ul class="list-group list-group-flush">

                      {% for user in admin_users %}

                          <li class="list-group-item">
                              <a href="" class="btn btn-xs btn-danger float-right"><i class="fa fa-close"></i> Delete User</a>
                              <h4>{{ user.username }}</h4>
                              <p class="font-xs font-gray">{{ user.email }}</p>
                          </li>

                      {% endfor %}
                  </ul>

              </div>
          </div>

      </div>

  </div>


{% endblock %}